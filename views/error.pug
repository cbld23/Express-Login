html(lang="es")
head
  meta(charset="UTF-8")
  meta(name="viewport" content="width=device-width, initial-scale=1.0")
  title Error
  link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css")
body
  .container-fluid
    .row.justify-content-center.align-items-center.min-vh-100
      .col-12.col-md-6.col-lg-4
        .card.shadow-sm
          .card-body
            h2.card-title.text-center.text-danger Error
            .alert.alert-warning(role="alert")
              | Por favor, confirma tu correo electrónico antes de iniciar sesión.
            a.btn.btn-warning.w-100(href="/auth/login") Volver a iniciar sesión
